#!/bin/bash

cache="$HOME/.cache/ssh-launch.cache"

flag_history='yes'
flag_cache='yes'

config_menu=('Clear History' 'History' 'Cache')

urxvt -e ssh `cat ~/.ssh/config | grep "^Host" | awk '{ print $2 }' | dmenu`
